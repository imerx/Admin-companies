<% if @payment.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody').prepend(' <%= j render "pay_row", payment: @payment, company_id:@company %>');
  $('.alert-success').html("El pago ha sido creado con éxito").show();
  $('#payment-modal').modal('hide');
<% else %>
  // show first error
  $('.alert').html('<%= @payment.errors.full_messages[0] %>').show();
<% end %>